import {View, ScrollView, Text, FlatList} from "react-native"


export default function CarouselTopics() {
    const testArray = ["text", "home", "profile", "text"]
    return <ScrollView className="flex h-10 flex-row gap-x-5 overflow-x-auto">
        {/* map over all of the topics */}
    
    
    </ScrollView> 
}